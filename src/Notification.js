import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Notification = () => {
  return (
    <View style={{flex:1,justifyContent:'center',backgroundColor:'#0198c7'}}>
      <Text style={{textAlign:'center',color:'#fff'}}>Notification</Text>
    </View>
  )
}

export default Notification

const styles = StyleSheet.create({})